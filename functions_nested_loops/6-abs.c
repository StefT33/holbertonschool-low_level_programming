#include <stdlib.h>
#include "main.h"
/**
* _abs - computes absolute value of an int
* @n: number to be checked
* Return: absolute value of n
*/

int _abs(int n)
{
if (n >= 0)
return (n);
else
return (-n);
}
