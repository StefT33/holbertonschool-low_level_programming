#include <stdio.h>
#include "main.h"
/**
* _isdigit - check for a digit (0 through 9)
*@c: digit to check
* Return: 1 or 0.
*/
int _isdigit(int c)
{
c = 0;
if (c <= 9)
{
return (1);
}
else
{
return (0);
}
}
